/*
#include<stdio.h>
#include<ctype.h>

int main(void){
    int n, q;
    scanf("%d%d", &n, &q);
    
    char input[16*n];
    char trash[n+1];
    
    scanf("%s", input);
    
    for(int i=0; i<16*n; i++){
        if(isalnum(input[i])){
            trash[i+1]=input[i];
        }
    }
    
    for(int i=0; i<q; i++){
        int l, r;
        scanf("%d%d", &l, &r);
        int ans = trash[l]+trash[r]-'0'-'0';
        printf("%d\n", ans);
        i++;
    }
    
    
    

    
    
    return 0;}
    */
/*
#include<stdio.h>
#include<ctype.h>

int main(void){
    int n, q;
    
    scanf("%d%d", &n, &q);
    char trash[n];
    char dump[15*n];
    
    int i=0;
    while(i != n){
        char k;
        if(isdigit(k = getchar())){
            trash[i]=k;
            i++;
        }
        else{
            dump[i]=getchar();
        }
    }
    
    for(int a=0; a<n; a++){
        printf("%d ", trash[a]);
    }
    /*
    for(int j=0; j<q; j++){
        int l, r;
        scanf("%d%d", &l, &r);
        printf("%d%d\n", trash[l],trash[r]);
    }
    */
    return 0;}
*/
