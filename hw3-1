/*
#include<stdio.h>
#include<ctype.h>

int main(void){
    int n, q;
    scanf("%d%d", &n, &q);
    
    char input[16*n];
    char trash[n+1];
    
    scanf("%s", input);
    
    for(int i=0; i<16*n; i++){
        if(isalnum(input[i])){
            trash[i+1]=input[i];
        }
    }
    
    for(int i=0; i<q; i++){
        int l, r;
        scanf("%d%d", &l, &r);
        int ans = trash[l]+trash[r]-'0'-'0';
        printf("%d\n", ans);
        i++;
    }
    
    
    

    
    
    return 0;}
    */
