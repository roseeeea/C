#include<stdio.h>

int main(void){
    int t;
    
    scanf("%d", &t);
    
    while(t--){
        int n;
        int ans[2];
        
        scanf("%d", &n);
        int a[n];
        
        for(int i=0; i<n; i++){
            scanf("%d", &a[i]);
        }
        
        int x=0;
        for(int i=0; i<n; i++){
            for(int j=0; j<n; j++){
                if(i!=j){
                    if (a[i]==a[j]){
                        break;
                    }
                }
                if(j==(n-1)){
                    ans[x]=a[i];
                    x++;
                }
                
            }
            if(x==2){
                break;
            }
                
        }
        
        if(ans[0]>ans[1]){
            printf("%d %d\n", ans[1], ans[0]);
        }
        else{
            printf("%d %d\n", ans[0], ans[1]);
        }
    }
    
    return 0;
}
